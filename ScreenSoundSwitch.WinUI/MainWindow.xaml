<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScreenSoundSwitch.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScreenSoundSwitch.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:ScreenSoundSwitch.WinUI.Controls"
    xmlns:viewModel="using:ScreenSoundSwitch.WinUI.ViewModels"
    xmlns:view="using:ScreenSoundSwitch.WinUI.Views"
    mc:Ignorable="d"
    >
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <NavigationView 
            Grid.Row="0"
            x:Uid="Nav"
            x:Name="Nav"
            SelectionChanged="{x:Bind NavigationSelectionChanged}"
            IsBackButtonVisible="Collapsed"                    
                        >
            <NavigationView.MenuItems >
                <NavigationViewItem x:Uid="SelectDevicePage" Tag="SelectDevicePage" >
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC77;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem  x:Uid="VolumePage" Tag="VolumePage" >
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF4C3;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem   x:Uid="AudioPage" Icon="Audio" Tag="AudioPage">
                    <!--
                                        <NavigationViewItem.MenuItems>
                        <NavigationViewItem Content="PlayList" Tag="PlayListPage" >
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE728;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                    </NavigationViewItem.MenuItems>
                    -->

                </NavigationViewItem>
                
            </NavigationView.MenuItems>
            <Frame x:Name="navContentFrame" Margin="0,20,0,0"/>
        </NavigationView>


     </Grid>
    
</Window>
