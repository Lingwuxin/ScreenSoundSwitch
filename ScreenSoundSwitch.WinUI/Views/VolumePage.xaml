<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ScreenSoundSwitch.WinUI.Views.VolumePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScreenSoundSwitch.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollView>
        <Grid Padding="20,12,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="980" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" Orientation="Vertical" Spacing="12">
                <InfoBar x:Name="VolumeStatusInfoBar"
                         IsOpen="True"
                         IsClosable="False"
                         Severity="Informational"
                         Message="正在初始化音频设备会话..."/>

                <StackPanel Spacing="2">
                    <TextBlock x:Uid="OutputDevicesTextBlock" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                    <TextBlock Text="管理每个输出设备及其会话音量" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <StackPanel x:Name="DevicesStackPanel" Orientation="Vertical" Spacing="8"/>
            </StackPanel>
        </Grid>
    </ScrollView>

</Page>
