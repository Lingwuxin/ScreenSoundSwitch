<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ScreenSoundSwitch.WinUI.Views.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScreenSoundSwitch.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:ScreenSoundSwitch.WinUI.Controls"
    xmlns:viewModel="using:ScreenSoundSwitch.WinUI.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewModel:UserViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="UserLoginStackPanel" Grid.Row="1" Grid.Column="0" Orientation="Vertical" >
            <controls:ContentControl>
                <controls:ContentControl.Icon>
                    <FontIcon Glyph="&#xE715;" />
                </controls:ContentControl.Icon>
                <controls:ContentControl.LeftContent>
                    <TextBlock Text="邮箱" />
                </controls:ContentControl.LeftContent>
                <controls:ContentControl.RightContent>
                    <TextBox PlaceholderText="例如：hello@gmail.com" x:Name="EmailInput" Text="{ Binding User.Username }"/>
                </controls:ContentControl.RightContent>
            </controls:ContentControl>
            <controls:ContentControl>
                <controls:ContentControl.Icon>
                    <FontIcon Glyph="&#xE77B;" />
                </controls:ContentControl.Icon>
                <controls:ContentControl.LeftContent>
                    <TextBlock Text="用户名" />
                </controls:ContentControl.LeftContent>
                <controls:ContentControl.RightContent>
                    <TextBox PlaceholderText="请输入用户名" x:Name="UserNameInput" Text="{ Binding User.Username }"/>
                </controls:ContentControl.RightContent>
            </controls:ContentControl>
            <controls:ContentControl>
                <controls:ContentControl.Icon>
                    <FontIcon Glyph="&#xE9A8;" />
                </controls:ContentControl.Icon>
                <controls:ContentControl.LeftContent>
                    <TextBlock x:Uid="PasswordTextBlock" />
                </controls:ContentControl.LeftContent>
                <controls:ContentControl.RightContent>
                    <PasswordBox PlaceholderText="请输入密码"  x:Name="PasswordInput" PasswordChar="*" Password="{Binding User.Password}" />
                </controls:ContentControl.RightContent>
            </controls:ContentControl>
            <controls:ContentControl>
                <controls:ContentControl.LeftContent>
                    <TextBlock Text="最后一步"/> 
                </controls:ContentControl.LeftContent>
                <controls:ContentControl.RightContent>
                    <Button Content="注册" HorizontalAlignment="Center" VerticalAlignment="Center" Click="RegisterButton_Click" />
                </controls:ContentControl.RightContent>
            </controls:ContentControl>
        </StackPanel>
    </Grid>
</Page>
